<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.debut.androidfeatures">

    <application
            android:name=".InitApplication"
            android:allowBackup="true"
            android:icon="@drawable/ic_feature"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme" android:fullBackupContent="true">
        <activity android:name=".SettingActivity">
        </activity>
        <activity android:name=".jetPackNavigation.JetPackActivity">
        </activity>
        <activity android:name=".ActivityWorkManager"/>
        <activity android:name=".recycleViewSample.ActivityRecyclerView"/>
        <activity android:name=".NightThemeActivity"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>